{% extends 'base.html.twig' %}
{% block title %}Mes expositions!{% endblock %}
{% block body %}
      {# NAV  #}
    <div class="menu">
        <div class="bar"></div>
    </div>
    <div class="overlay"></div>
    <header>
        {% include 'nav.html.twig' %}
    </header>

    <div>
        <h3 class="text-center mt-5 h3-bio">{{ exposition.getTitre }}</h3>

        <img src="{{ exposition.getImage }}" alt="{{ exposition.titre }}"><br>
        {{ exposition.getLieu }}<br>
        {{ exposition.getHoraire }}<br>
        {{ exposition.getDescription }}<br>
    <div>
{% endblock %}
