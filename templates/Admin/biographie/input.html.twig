{% extends 'base.html.twig' %}

{% block body %}
    {# NAV  #}
    <div class="menu">
        <div class="bar"></div>
    </div>
    <div class="overlay"></div>
<header>
    {% include 'nav.html.twig' %}
</header>
    <h1 class="text-center mt-4">Votre biographie</h1>

    {{ form_start(form) }}
    {{ form_row(form.photo) }}
    <img src="{{ asset('/images/biographie/' ~ original_image) }}">
    {{ form_row(form.description) }}
    <div class="text-right">
        <button type="submit" class="btn btn-primary">
            Modifier
        </button>
    </div>
    {{ form_end(form) }}

{% endblock %}