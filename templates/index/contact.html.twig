{% extends 'base.html.twig' %}

{% block body %}
    <body style="background-color: #e6e6e6;">
    {# NAV  #}
    <div class="menu">
        <div class="bar"></div>
    </div>
    <div class="overlay"></div>
    <header>
        {% include 'nav.html.twig' %}
    </header>

<div class="container mt-3">
    <div class="row">
        <div class="col-8 mx-auto text-center form-contact mt-2">
            <h1 class="mt-5">Contactez moi !</h1>
            {{ form_start(form) }}
            {{ form_row(form.name) }}
            {{ form_row(form.email) }}
            {{ form_row(form.subject) }}
            {{ form_row(form.body) }}
    <div class="text-center">
        <button type="submit" class="contact75-form-btn mx-auto ml-5"><i class="far fa-paper-plane"></i>
            Envoyer
        </button>
    </div>
    {{ form_end(form) }}
        </div>
    </div>
</div>
    </body>
{% endblock %}

