{% extends 'base.html.twig' %}

{% block title %}Mon Blog{% endblock %}

{% block body %}
    {# NAV  #}
    <div class="menu">
        <div class="bar"></div>
    </div>
    <div class="overlay"></div>
    <header>
        {% include 'nav.html.twig' %}
    </header>

    <div>
        <h3 class="text-center mt-5 h3-bio">Mes articles blog</h3>

        {% for blog in blogs %}
            {{ blog.titre }}
            {{ blog.contenu }}
            {{ blog.image }}
            {{ blog.datePublication|date('d/m/Y H:i') }}
        {% endfor %}


    </div>
{% endblock %}
